project Main is
   for Source_Dirs use ("src");
   for Main use ("main.adb");
   for Exec_Dir use "bin";

      package Compiler is
      for Default_Switches ("ada") use ("-gnatwa");
   end Compiler;
   
end Main;

